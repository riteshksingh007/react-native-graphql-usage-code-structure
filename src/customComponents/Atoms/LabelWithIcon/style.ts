import { StyleSheet } from 'react-native';

const LabelWithIconStyles = StyleSheet.create({
  fieldLabel: {
    fontSize: 14,
    color: '#555',
    fontFamily: 'AvenirNext-Bold',
    marginLeft: 8,
  },

  mainView: {
    flexDirection: 'row',
    alignItems: 'center'
  }
});

export default LabelWithIconStyles;
